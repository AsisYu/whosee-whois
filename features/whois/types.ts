/**
 * WHOIS Types
 * Re-exports WHOIS types from lib/types/api
 * This maintains backward compatibility while respecting architecture boundaries
 */

export type { WhoisData, WhoisMeta, WhoisResponse } from '@/lib/types/api'
